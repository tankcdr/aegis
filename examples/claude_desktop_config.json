{
  "_comment": "Aegis Protocol MCP server for Claude Desktop.",
  "_instructions": [
    "1. Build the server: git clone https://github.com/tankcdr/aegis && cd aegis && pnpm install && pnpm -r build",
    "2. Replace <PATH_TO_AEGIS> with the actual path to your clone.",
    "3. Add this block to ~/Library/Application Support/Claude/claude_desktop_config.json (macOS)",
    "   or %APPDATA%/Claude/claude_desktop_config.json (Windows)",
    "4. Restart Claude Desktop.",
    "5. You now have three tools: trust_query, should_proceed, trust_explain"
  ],
  "mcpServers": {
    "aegis": {
      "command": "node",
      "args": ["<PATH_TO_AEGIS>/packages/mcp/dist/index.js"],
      "env": {
        "GITHUB_TOKEN": "<YOUR_GITHUB_TOKEN>"
      }
    }
  }
}
