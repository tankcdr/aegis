# API base URL — points to the Fastify API (api.trstlyr.ai in production)
NEXT_PUBLIC_API_URL=https://api.trstlyr.ai

# For local dev, point at local API:
# NEXT_PUBLIC_API_URL=http://localhost:3000
